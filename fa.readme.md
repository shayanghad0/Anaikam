# Local ChatBot (راهنمای فارسی)

چت‌بات هوش مصنوعی کیفیت‌بالا و سازگار با هر ارائه‌دهنده‌ای (شبیه ChatGPT/Claude) که کاملاً روی سیستم شما اجرا می‌شود. ورود تک‌مالک، دیتابیس JSON، پاسخ استریمی، جستجوی وب، تفکر عمیق، پیوست‌ها و مارکداون غنی.

## امکانات

- **هر API سازگار با OpenAI** — OpenAI، OpenRouter، Groq، DeepSeek، Ollama، LM Studio و…
- **چت استریمی** با توقف، بازتولید، ادامه، ویرایش و حذف
- **حالت جستجوی وب** — نتایج DuckDuckGo در پرامپت تزریق می‌شود؛ فهرست منابع زیر پاسخ
- **حالت تفکر عمیق** — پرامپت استدلال دقیق + بلوک جمع‌شونده «فرایند فکری»
- **پیوست** — تصویر، PDF، متن/مارکداون (سقف ۲۰ مگابایت)
- **مارکداون غنی** — GFM، هایلایت کد، ریاضی KaTeX، دیاگرام Mermaid
- **گفتگوها** — ساخت، تغییر نام، حذف، گروه‌بندی، جستجو، ورود/خروج (MD / JSON / TXT)
- **تنظیمات** — ارائه‌دهنده AI، حساب (تغییر رمز با رمز فعلی)، ظاهر، نمایش چت
- **ورود تک‌مالک** — پیش‌فرض `admin` / `admin123`؛ کوکی نشست؛ ثبت‌نام عمومی ندارد
- **تم تیره کاملاً سیاه** به‌صورت پیش‌فرض
- **فهرست پیام‌های مجازی‌شده** برای گفتگوهای طولانی
- **کلید API فقط روی سرور** — مرورگر فقط با API محلی حرف می‌زند

## فناوری

| لایه | تکنولوژی |
|------|----------|
| فرانت‌اند | Vite، React 18، TypeScript، Tailwind CSS 4 |
| بک‌اند | Express (فقط API محلی) |
| ذخیره‌سازی | فایل‌های JSON در `database/` |

## پیش‌نیاز

- Node.js 18+ (تست‌شده روی Node 24)
- کلید API سازگار با OpenAI یا اندپوینت محلی

## شروع سریع

```bash
npm install
npm run dev
```

- برنامه: http://localhost:5173  
- API: http://127.0.0.1:3001  

با حساب پیش‌فرض وارد شوید، سپس **Settings → AI** را باز کنید و تنظیم کنید:

1. **API Base URL** (مثلاً `https://api.openai.com/v1`)
2. **API Key**
3. **Model** (مثلاً `gpt-4o-mini`)

قبل از چت، **Test connection** را بزنید.

### بیلد نهایی

```bash
npm run build
npm start
```

### بررسی تایپ

```bash
npm run typecheck
```

## دستورات

| دستور | توضیح |
|-------|--------|
| `npm run dev` | API + Vite با هم |
| `npm run dev:server` | فقط Express |
| `npm run dev:client` | فقط Vite |
| `npm run build` | تایپ‌چک + باندل production |
| `npm start` | اجرای سرور production |
| `npm run typecheck` | بررسی پروژه TypeScript |

## ساختار پروژه

```
├── server/          # API اکسپرس، احراز هویت، پراکسی AI، جستجوی وب، JSON DB
├── shared/          # تایپ‌ها و مقادیر پیش‌فرض مشترک
├── src/             # اپ React (کانتکست، UI چت، تنظیمات، مارکداون)
├── database/        # ساخت خودکار: تنظیمات، چت‌ها، پیوست‌ها، نشست‌ها
└── index.html       # نقطه ورود + اسکریپت تم تیره
```

## حالت‌ها

### جستجو

آیکون **گِلوب** را در کادر پیام روشن کنید. هنگام ارسال، سرور DuckDuckGo را جستجو می‌کند، نتایج را در پرامپت می‌گذارد و فهرست **Sources** زیر پاسخ برمی‌گرداند.

### تفکر عمیق

آیکون **مغز** را روشن کنید. سرور پرامپت استدلال دقیق را اضافه می‌کند؛ اگر مدل `reasoning_content` بفرستد، در بلوک جمع‌شونده **Thought process** نمایش داده می‌شود.

هر دو پیش‌فرض در **Settings → Chat** قابل تنظیم‌اند.

## نکات امنیتی

- بعد از اولین ورود رمز پیش‌فرض را عوض کنید (**Settings → Account**).
- تغییر رمز، رمز فعلی را می‌خواهد (مقایسه timing-safe).
- `GET /api/config` هرگز رمز و کلید API را برنمی‌گرداند (فقط `hasApiKey`).
- فراخوانی‌های AI از سمت سرور پراکسی می‌شوند تا کلید در مرورگر نماند.
- اگر دسترسی LAN نمی‌خواهید، API را فقط به `127.0.0.1` وصل کنید.

## داده‌ها

همه‌چیز در `database/`:

- `config.json` — تنظیمات و احراز هویت  
- `chats/*.json` — گفتگوها  
- `attachments/` — فایل‌های آپلودشده  
- `sessions.json` — نشست‌های ورود  

فایل‌های ناخواسته را حذف کنید؛ ساختار در شروع دوباره ساخته می‌شود.

## لایسنس

[MIT](LICENSE)

## امنیت

گزارش آسیب‌پذیری و نکات امنیتی: [SECURITY.md](SECURITY.md). بعد از اولین ورود رمز پیش‌فرض را عوض کنید.
