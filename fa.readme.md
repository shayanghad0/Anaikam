# انایکام — برنامه چت هوش مصنوعی

یک برنامه چت هوش مصنوعی آمادهٔ تولید با رابط کاربری مدرن.

## نام سایت

**انایکام**

---

## تکنولوژی‌ها

| لایه | فناوری |
|------|--------|
| بک‌اند | Node.js · Express.js · TypeScript |
| فرانت‌اند | React · Vite · TypeScript |
| پایگاه داده | فایل‌های JSON (ایجاد خودکار هنگام اولین اجرا) |

**استفاده نشده:** MongoDB، MySQL، SQLite，PostgreSQL, Redis, IndexedDB, LocalStorage, SessionStorage. تمام ذخیره‌سازی سمت سرور و در فایل‌های JSON است.

---

## ساختار پروژه

```
/client
  /components
  /pages
  /hooks
  /services
  /types
  /utils

/server
  /routes
  /controllers
  /middlewares

/shared

/database
  users.json
  sessions.json
  settings.json
  models.json
  chats.json
  messages.json
  system-prompts.json
  themes.json
  logs.json
```

تمام فایل‌های پایگاه‌داده هنگام اولین راه‌اندازی به‌طور خودکار ساخته می‌شوند.

---

## ویژگی‌ها

### احراز هویت
- ورود / خروج / ثبت‌نام
- فراموشی رمز عبور (بازیابی توسط مدیر)
- احراز هویت مبتنی بر جلسه (Session)
- هش رمز با bcrypt
- نقش‌ها: مدیر (Admin)، کاربر (User)

### چت
- پاسخ‌های ریزشی (Streaming) با قابلیت توقف، ادامه و تکرار
- ویرایش پیام کاربر، حذف پیام‌ها
- کپی پیام و کپی کد
- رندر Markdown با تشخیص سینتکس
- جدول، ریاضیات LaTeX، نمودارهای Mermaid
- نمایش تصاویر از طریق URL
- انیمیشن تایپ و نشانگر پردازش
- عنوان‌گذاری خودکار گفتگوها و امکان تغییر نام
- چت‌های سنجاق‌شده (Pinned) و آرشیوو‌شده
- گفتگوهای نامحدود با جستجو
- نمایش زمان هر پیام

### ارائه‌دهندگان مدل
پشتیبانی از هر API سازگار با OpenAI:
- OpenAI · OpenRouter · DeepSeek · سازگار با Anthropic
- سازگار با Google Gemini · Ollama · LM Studio · vLLM · LiteLLM

قابل تنظیم برای هر کاربر: آدرس API، توکن، مدل، هدرهای سفارشی

### چندمدلی
تغییر مدل داخل گفتگو (GPT, Claude, Gemini, DeepSeek, Qwen, Llama, Mistral و…)

### سیستم‌پراپت‌ها
سیستم‌پراپت سفارشی نامحدود با:
- کتابخانه پراپت (مترجم، برنامه‌نویس، معلم، پزشکی، حقوقی، تجاری، نویسنده…)
- علاقه‌مندی‌ها، وارد/صادر کردن، Duplicate، دسته‌بندی

### پرسوناهای هوش مصنوعی
ساخت پرسونای اختصاصی با نام، تصویر پروفایل، توضیحات، سیستم‌پراپت، دما، خلاقیت، مدل ترجیحی و دسته‌بندی.

### تنظیمات (قابل تنظیم از طریق رابط کاربری)
نام نمایشی، تصویر پروفایل، تم، زبان، منطقه زمانی، اندازه فونت، عرض پیام، ارسال با Enter، جریان‌دارسازی، انیمیشن‌ها، مارک‌داون، برجسته‌سازی کد، دما، Top-P، حداکثر توکن، تأخیر، فرمت خروجی، عنوان‌گذاری خودکار، پیش‌فرض سیستم‌پراپت.

### جستجو
جستجوی گفتگوها، پیام‌ها، کتابخانه پراپت و پرسوناهارا.

### خروجی گرفتن (Export)
صادرات گفتگوها به فرمت‌های Markdown, PDF, HTML, JSON, TXT. وارد کردن گفتگوهای قبلی.

### طراحی رابط کاربری
- حالت تاریک (Dark Mode) و روشن (Light Mode) مدرن
- طرح‌بندی واکنش‌گرا با سایدبار
- ناوبری بالایی، افکت شیشه‌ای (Glassmorphism)
- انیمیشن‌های نرم فیزیک فنری
- پشتیبانی از موبایل

### پروفایل
آواتار، نام کاربری، نام نمایشی، بیو، تم، رمز عبور، ایمیل، زبان.

### پنل مدیریت
داشبورد، مدیریت کاربران، مدیریت مدل‌ها، لاگ‌ها، تنظیمات، اطلاعیه‌ها، آمار، تنظیمات API، خاموش/روشن کردن ثبت‌نام.

---

## امنیت

- هش کردن رمز عبور با bcrypt
- محدود کردن نرخ درخواست (Rate Limiting)
- Helm.js برای هدرهای امنیتی
- محافظت CORS
- اعتبارسنجی ورودی (Zod)
- محافظت در برابر XSS
- محافظت CSRF (در موارد مناسب)
- اعتبارسنجی مسیر
- مدیریت امن نشست‌ها

---

## کیفیت کد

- TypeScript سخت‌گیرانه (بدون `any`)
- معماری تمیز (Clean Architecture)
- اصول SOLID
- کامپوننت‌های ماژولار و قابل استفاده مجدد
- بدون کد تکراری

---

## شروع به کار

```bash
npm install
npm run dev
```

هنگام اولین راه‌اندازی برنامه به‌طور خودکار موارد زیر ساخته می‌شوند:
- پوشه `/database` شامل تمام فایل‌های JSON
- حساب مدیر پیش‌فرض
- تنظیمات و پیکربندی مدل پیش‌فرض

---

## اصل طراحی اصلی

این **فقط یک برنامه چت هوش مصنوعی** است. شامل موارد زیر **نیست**:
- ترمینال، مرورگر فایل، فضای کاری پروژه
- یکپارچگی Git، اجرای کد، دستورات Shell
- ابزارهای ایجنت، ویرایش فایل، ویژگی‌های دستیار کدنویسی
- دسترسی به فایل‌سیستم مرورگر

فرانت‌اند هیچ داده‌ای را به‌صورت دائمی ذخیره نمی‌کند — همه چیز از پایگاه‌داده JSON بک‌اند تأمین می‌شود.
